# 开发日志 — 2026-06-03

> 上一篇：`devlog/2026-06-02.md`（Phase 0 + Phase 1）

## 今日完成

### Phase 1 验收
- [x] 用户在 Xcode 中 ⌘R 运行，确认菜单栏图标、空 Popover、点击外部关闭、Dock 无图标 —— 4 项全部通过

### Phase 2 — CoreData 数据层
- [x] 创建 CoreData 模型 `ClipHistory.xcdatamodeld`，定义实体 `ClipItemEntity`
  - 字段：id(UUID)、typeRaw(String)、content(String?)、imageData(Binary, 外部存储)、createdAt(Date)、isPinned(Bool)、appSource(String?)
  - codeGenerationType = class（Xcode 自动生成 NSManagedObject 子类）
- [x] 编写 `PersistenceController.swift`（CoreData stack 单例）
  - store 存储位置指向 `~/Library/Application Support/ClipHistory/ClipHistory.sqlite`
  - 启动时自动创建目录、合并策略 NSMergeByPropertyObjectTrumpMergePolicy
- [x] 在 `AppDelegate` 启动时引用 `PersistenceController.shared`，触发 store 加载
- [x] 更新 `project.pbxproj`：注册 PersistenceController.swift + xcdatamodeld（XCVersionGroup）
- [x] 命令行编译：`** BUILD SUCCEEDED **`
- [x] 运行验证：sqlite 文件生成成功，`sqlite3 .tables` 显示 `ZCLIPITEMENTITY`，7 个字段全部正确

---

## 当前状态

**开发阶段**：Phase 2（数据层）— 全部验收通过 ✅

**Phase 2 验收标准**：
- [x] 编译无报错
- [x] 运行后在 `~/Library/Application Support/ClipHistory/` 生成 `.sqlite` 文件

---

## 待办（下次会话开始处）

- [ ] **Phase 3 — 监听服务**：剪贴板轮询 + 隐私检测 + 写入
  - 编写 `Models/ClipItemType.swift`（.text / .image 枚举）
  - 编写 `Services/ClipboardMonitor.swift`（500ms 轮询、changeCount 比对、隐私检测、去重、写入 CoreData）
  - 在 AppDelegate 启动 monitor，退出时清理 timer
  - 验证：复制文字 → 控制台打印日志；重复复制去重；密码管理器内容跳过

---

## 问题与决策记录

| 问题 | 决策 | 记录时间 |
|---|---|---|
| CoreData 类生成方式 | codeGenerationType=class，自动生成 ClipItemEntity，不手写实体类 | 2026-06-03 |
| store 路径设置 | 在 PersistenceController.init 中手动设置 description.url 指向 Application Support/ClipHistory | 2026-06-03 |
| 触发 store 加载时机 | AppDelegate.applicationDidFinishLaunching 中引用 viewContext | 2026-06-03 |
